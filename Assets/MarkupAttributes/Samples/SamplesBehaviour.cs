using UnityEngine;

namespace MarkupAttributes.Samples
{
    // This thing is here just to easily apply custom editor
    // to all sample scripts, without applying it also
    // to all scripts in the project without user explicitly
    // deciding to do so.
    public class SamplesBehaviour : MonoBehaviour
    {
    }
}
